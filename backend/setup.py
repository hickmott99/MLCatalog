from setuptools import setup

setup(name='MLCatalog',
      version='1',
      description='A backend flask Rest API',
      author='Cooper Hickmott',
      packages=['MLCatalog'],
      zip_safe=False)